# ingress.yaml
# Content for /resources/ingress.yaml

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: resource-name
  labels:
    app: component-name
    version: v1.0.0
    component: ingress
spec:
  # Add Ingress specific configuration here